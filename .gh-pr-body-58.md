## Summary
Wire Slack notification delivery end-to-end so the package routes alerts through the configured channels and validates Slack webhook setup in the doctor command. Closes #58.

## Changes
- add Slack webhook configuration and normalize notification channel / recipient parsing
- route notifications through combined on-demand mail and Slack channels in `SendSecurityAlert`
- extend `crowdsec:doctor` to fail when configured mail or Slack routes are incomplete
- document Slack setup in `README.md`
- add notification and doctor coverage for mail+slack, slack-only, and missing webhook scenarios

## Testing
- [x] Updated notification and doctor feature tests
- [x] Existing test suite still passes

## Checklist
- [x] Code follows existing project conventions
- [ ] `vendor/bin/pint --dirty --format agent` was run (`vendor/bin/pint` is not installed in this repository)
- [x] No unrelated changes were included
