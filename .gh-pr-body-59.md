## Summary
Align the metrics endpoint fixtures with the current schema and model contracts so the tests validate real runtime behavior instead of relying on stale fields. Closes #59.

## Changes
- replace stale `blocked_until` fixture usage with `expires_at`
- remove unsupported `description` payloads from `SecurityEvent` test records
- add helper builders so metrics fixtures consistently use valid model attributes
- strengthen the blocked IP total assertion to verify the actual metric value

## Testing
- [x] Updated metrics endpoint tests
- [x] Existing test suite still passes

## Checklist
- [x] Code follows existing project conventions
- [ ] `vendor/bin/pint --dirty --format agent` was run (`vendor/bin/pint` is not installed in this repository)
- [x] No unrelated changes were included
