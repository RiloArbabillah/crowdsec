## Summary
Add a lightweight static analysis quality gate so CI validates Composer metadata and runs PHPStan alongside the existing PHPUnit matrix. Closes #60.

## Changes
- add PHPStan and Larastan as dev dependencies plus a `composer analyse` script
- add `phpstan.neon`, a generated baseline, and Slack notification stubs for repository-aware analysis
- add a dedicated `quality` GitHub Actions job for `composer validate` and static analysis
- ignore PHPStan cache output and document the new local analysis workflow in `README.md`

## Testing
- [x] `composer analyse`
- [x] `composer test`
- [x] `composer validate --strict`

## Checklist
- [x] Code follows existing project conventions
- [ ] `vendor/bin/pint --dirty --format agent` was run (`vendor/bin/pint` is not installed in this repository)
- [x] No unrelated changes were included
