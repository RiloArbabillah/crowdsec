## Summary
Finish the final v1 readiness gap by making audit retention operational in `crowdsec:cleanup`, so the remaining audit configuration is no longer just a documented setting. Closes #55.

## Changes
- extend `crowdsec:cleanup` to prune audit log entries older than `audit.retention_days`
- add feature coverage for audit retention cleanup and dry-run behavior
- document audit cleanup behavior and retention configuration in `README.md`

## Testing
- [x] `vendor/bin/phpunit tests/Feature/AuditLogTest.php`
- [x] `composer analyse`
- [x] `composer test`

## Checklist
- [x] Code follows existing project conventions
- [ ] `vendor/bin/pint --dirty --format agent` was run (`vendor/bin/pint` is not installed in this repository)
- [x] No unrelated changes were included
